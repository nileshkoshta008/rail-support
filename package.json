{
  "name": "scalar-rail-support",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:api\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:api": "python api/index.py"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
